package com.tourpackage.api.entity;

public enum BannerPlacement {
    /** The rotating hero slider on the homepage. */
    HOME_SLIDER,
    /** Promotional strip above the offers section. */
    OFFERS_STRIP,
    /** Static hero image at the top of a listing page. */
    PAGE_HERO
}
